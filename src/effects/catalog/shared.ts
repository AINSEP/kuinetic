/** Re-exported for same-directory catalog siblings; the canonical definitions live in `effects/shared.ts` — neutral ground for every effect category, not just `catalog/`. */
export {
  cssPrimitive,
  TIMELINE_AGNOSTIC,
  TRIGGER_DELAY_PARAM,
  withTimingContract,
} from '../shared.js'
